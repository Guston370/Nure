<?xml version="1.0" encoding="utf-8"?>
<!-- Enhanced bottom navigation text selector with health theme -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    
    <!-- Selected state - adaptive primary color -->
    <item android:state_checked="true">
        <color android:color="@color/bottom_nav_text_active" />
    </item>
    
    <!-- Pressed state - adaptive primary color -->
    <item android:state_pressed="true">
        <color android:color="@color/bottom_nav_text_active" />
    </item>
    
    <!-- Focused state -->
    <item android:state_focused="true">
        <color android:color="@color/bottom_nav_text_active" />
    </item>
    
    <!-- Default state - adaptive secondary color -->
    <item>
        <color android:color="@color/bottom_nav_text_inactive" />
    </item>
    
</selector>

